import React from 'react'
import Page from 'components/page/Page'
import { siteUrl } from 'site-metadata'

const SomePage = () => (
  <Page
    pageUrl={siteUrl + <%= JSON.stringify(attributes.slug) %>}
    pageTitle={<%= JSON.stringify(attributes.title) %>}
    pageDate={<%= JSON.stringify(attributes.date) %>}
    pageBody={<%= JSON.stringify(body) %>}
  />
)

export default SomePage
